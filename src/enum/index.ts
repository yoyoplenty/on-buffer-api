export enum UserStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}

export enum TokenType {
  PHONE_VERIFICATION = 'phone_verification',
  PIN_RESET = 'pin_reset',
}
